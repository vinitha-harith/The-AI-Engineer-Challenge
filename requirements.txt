# Requirements for Vercel deployment (no numpy due to 250MB limit)
fastapi>=0.115.12
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.11.4
pydantic-settings>=2.0.0
# Note: numpy excluded to stay under Vercel's 250MB limit
# Pure Python vector operations are used in index_vercel.py
